# Int

```scala
import scala.math

abs(-1) // 絶対値
max(1,2) // 最大値
min(1,2) // 最小値
pow(2, 10) // 乗数
sqrt(2.0) // 平方根

floor(9.9) //=> 9.0 (切り捨て)
ceil(9.9) //=> 10.0 (切り上げ)
round(9.9) //=> 10 (四捨五入)

E // 自然対数
Pi // 円周率
random //=> [0,1) の乱数
```

